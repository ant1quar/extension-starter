const popupConfig = require("./webpack.popup.config");
const backgroundConfig = require("./webpack.background.config");
const widgetConfig = require("./webpack.widget.config");

module.exports = [
    popupConfig,
    backgroundConfig,
    widgetConfig
];
